pipeline {
    agent any
    registry = 'docker-hub'
    registry-name = 'redaloukil'
    stages {
        stage('Linting') {
            steps {
                sh 'npm run lint'
            }
        }
        stage('Testing') {
            steps {
                sh 'npm run test'
            }
        }
        stage('pull in registry'){
            steps {
                sh ''
            }
        }
        stage('Deploy kubernetes'){
            steps {
                sh ''
            }
        }
    }
}
